package main

import "fmt"

func main() {
	print_table(10, 10)
}

func print_table(x, y int) {
	for i := 0; i < y; i++ {
		for j := 0; j < x; j++ {
			{
				if (i-j)%2 == 0 {
					fmt.Print(0)

				} else {
					fmt.Print(1)
				}

			}
		}
		fmt.Print("\n")
	}
}
